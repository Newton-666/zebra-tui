# Krystal TODO

## 待办

- [ ] **一句话建队确认页支持逐个编辑**：现在只能整体接受或重来；希望在确认页里改某成员的
      类型 / 模型 / 身份 / 职责
- [ ] **`:task <描述>` 派工**：一句话派活 → 写入白板任务区 + 按协议指派成员（建队→派工闭环）
- [ ] **未知命令纠错**：`:mdoel` 之类的拼写错误 → 提示「你是不是想用 `:model`」
- [ ] **会话管理命令**：`:sessions`（列出/切换/删除/导出），替代手工删目录
- [ ] **自动备份**：团队会话定期快照到 `sessions/.backup/`（见下方教训）
- [ ] **成员日志导出**：现在画面以「快照帧」形式留在内存与 history.jsonl；可加按成员导出纯文本日志
- [ ] **模型弹窗增强**：搜索过滤（模型多时翻列表累）、显示 token 价格/上下文（部分来源已有 ctx）
- [ ] **多列布局 >2 列**：目前只支持 2 列（成员 3–4 人时每列上下平分）

## 已完成

- [x] `:model` 弹窗（成员 → 来源 → 模型，overlay 选择器，v0.4.3）
- [x] `/` 与 `:` 等价；未知命令只提示、不广播给成员（v0.4.2）
- [x] 每格独立滚动：cell 累积画面快照（末帧原地更新，差异大才追加）（v0.4.1）
- [x] 模型来源按当前 provider 排序 + 标注其他来源（v0.4.1）
- [x] 建队时选模型 / 运行时 `:model <成员> <模型>` 切换（v0.4.0）
- [x] 逐成员身份 + 注入策略（新建/重建/复活才注入）（v0.3.1）
- [x] 初始界面（logo + 模式选择）、一句话建队、预置协议（v0.3.0）
- [x] 团队感知与通信：身份 + 简报 + `krystal` 协作命令 + 白板 + 中继可视化（v0.2.0）
- [x] 协作护栏：防「互相确认」循环（v0.2.1）

## 教训 / 约定

- **绝不对 `sessions/*` 做整体删除**（2026-09-23：一次收尾清理 `rm -rf sessions/*`
  误删了用户的团队记录 `team.json`/`history.jsonl`/`board.md`，agent 侧上下文虽在，
  Krystal 侧历史不可恢复）。测试产生的会话必须用可识别的名字（如 `*-test`），只删这些。
- 助手改动前后：先确认「要动的是测试数据还是用户数据」。